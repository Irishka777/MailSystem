package com.tsystems.javaschool.mailsystem.shareableObjects;

/*
 * Available actions for client
 */
public enum ToDo {
	Login, SendMessage, SaveMessage, DeleteMessage, CreateFolder, DeleteFolder, RenameFolder,
	MoveMessageToAnotherFolder, Update, CloseSocket
}
